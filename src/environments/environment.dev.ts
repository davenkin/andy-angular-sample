import { Environment } from 'common/model/common.model';

export const environment: Environment = {
  production: false,
  apiHost: 'https://api.dev.host.com',
  keycloakUrl: 'http://localhost:8080',
};
